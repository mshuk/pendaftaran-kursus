"use client";import {useState} from "react";
export default function Form({courseId}:{courseId:string}){const[done,setDone]=useState("");const[error,setError]=useState("");async function submit(e:any){e.preventDefault();setError("");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/registrations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...data,courseId})});const j=await r.json();if(!r.ok){setError(j.error||"Pendaftaran gagal");return}setDone(j.number)}if(done)return <div className="success"><h2>Pendaftaran berjaya</h2><p>Nombor pendaftaran:</p><h2>{done}</h2><p>Sila simpan nombor ini.</p></div>;return <form className="card" onSubmit={submit}><label className="field">Nama penuh<input name="name" required/></label><label className="field">E-mel<input name="email" type="email" required/></label><label className="field">No. telefon<input name="phone" required/></label><label className="field">Jabatan / Organisasi<input name="department" required/></label>{error&&<div className="error">{error}</div>}<button className="btn">Hantar Pendaftaran</button></form>}